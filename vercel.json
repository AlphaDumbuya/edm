{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "npm install && npx prisma generate && next build",
  "outputDirectory": ".next",
  "regions": ["sin1"],
  "functions": {
    "app/api/**/*.ts": {
      "memory": 1024,
      "maxDuration": 30
}
}
}
